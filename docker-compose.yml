services:
  shrinker:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
      # Mount your media directories below:
      - /home/coke/Downloads:/media/Videos
      # - /path/to/movies:/media/Movies
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
